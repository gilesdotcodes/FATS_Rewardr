<%= form_for @badge do |f| %>
  <%= f.label :employee %>
  <%= f.collection_select :user, User.all, :id, :name %>
  <%= f.label :badge %>
  <%= f.collection_select :name, Badge::BADGES, :to_s, :humanize %>
   <%= f.submit 'Reward' %>
<% end %>